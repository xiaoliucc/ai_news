# 项目进度

> 最后更新: 2026-08-20

---

## 当前阶段：Phase 5 全部完成（前端接入 + 主题优化 + 收官）

### 已完成

- [x] 项目需求讨论
- [x] 技术方案设计
- [x] 目录结构创建
- [x] `pyproject.toml` 配置（uv 管理 + CLI 入口 `ai-news`）
- [x] `.gitignore`（含 `.env` 密钥排除 + `data/` 数据库文件排除）
- [x] `README.md`
- [x] 依赖安装（httpx, click, arxiv, pytest, pytest-asyncio, openai, python-dotenv, apscheduler, fastapi, uvicorn, chromadb, sentence-transformers）
- [x] `src/models.py` — Article 数据类
- [x] `src/sources/base.py` — SourcePlugin 抽象基类
- [x] `src/sources/hackernews.py` — Hacker News 采集器
- [x] `src/sources/arxiv.py` — ArXiv 论文采集器
- [x] `src/sources/papers.py` — Papers With Code 采集器（Hugging Face API）
- [x] `src/sources/rss.py` — 通用 RSS/Atom 聚合器（RSS_FEEDS 配置）
- [x] `src/engine.py` — NewsEngine 调度引擎（并发采集 + 去重 + 排序 + 统计）
- [x] `src/filters.py` — AI 关键词过滤（70 中英文关键词，LLM 降级回退）
- [x] `src/output.py` — 三种输出格式（text / json / markdown）
- [x] `src/main.py` — CLI 入口（click 命令组）
- [x] `src/pipeline/ranking.py` — 复合排序（时间衰减 + 源权重 + 归一化）
- [x] `src/pipeline/llm.py` — LLM 语义判断 + 质量打分（DeepSeek，降级回退）
- [x] `backend/__init__.py` — 后端包标记
- [x] `backend/config.py` — 配置管理（.env 驱动，含 CHROMA_DIR）
- [x] `backend/database.py` — SQLite 数据层（3 表，10 函数，WAL 模式）
- [x] `backend/scheduler.py` — APScheduler 定时采集（采集时间步写入 ChromaDB）
- [x] `backend/vector_store.py` — ChromaDB 向量索引（惰性初始化，Unix 时间戳 metadata）
- [x] `backend/main.py` — FastAPI app（lifespan + CORS + 4 router + /health）
- [x] `backend/routers/articles.py` — GET /api/articles
- [x] `backend/routers/sources.py` — GET /api/sources（源元数据 + 文章数）
- [x] `backend/routers/stats.py` — GET /api/stats（采集统计 + 历史明细）
- [x] `backend/routers/agent.py` — POST /api/agent/chat（直接调用，索引由 scheduler 维护）
- [x] `backend/agent/prompts.py` — System prompt（日期锚点 + 用户偏好 + 语言策略）
- [x] `backend/agent/tools.py` — 5 工具全部实现（ChromaDB 语义搜索 + SQLite 回填摘要 / 文章详情 / LLM 摘要 / 趋势分析 / 触发采集）
- [x] `backend/agent/core.py` — Agent 主循环（tool-use，最多 5 轮，并发执行）
- [x] 源开关：`user_profile.selected_sources`（空=全选）+ `PUT /api/sources/{name}`
- [x] 国内源接入：机器之心官方 RSS 已配置进 `.env`（`RSS_FEEDS`）
- [x] `POST /api/collect` 手动采集端点（202 + 后台任务）
- [x] 前端正式接入：OpenDesign 复刻（Vue 3.5 + Vite + TS + Element Plus + ECharts + Pinia），真实 API 数据链路
- [x] 前端优化：VChart 注册修复 / echarts + Element Plus 按需引入（总包 -39%）/ mock 死代码清理
- [x] 记忆 P2：跨会话对话摘要（滚动窗口归档 + 增量合并 + prompt 注入）
- [x] **全部 docstring 为 Google 风格**
- [x] 测试目录重构：`src/tests/` → `tests/`（镜像分类）
- [x] CLAUDE.md — 项目架构文档
- [x] git 初始提交（6d7ee28 → github.com/xiaoliucc/ai_news）
- [x] 测试：**195 用例**（187 单元 + 8 集成）
  - 路由层: test_articles (7), test_sources (8), test_stats (4), test_agent (13), test_collect (2)
  - 数据/引擎层: test_models (1), test_engine (13), test_output (13), test_ranking (8), test_database (28), test_scheduler (11), test_vector_store (31), test_llm (14)
  - 源解析层: test_rss (12)
  - Agent 层: test_agent (4), test_tools (13), test_memory (5)
  - 集成测试: test_hackernews (1), test_arxiv (2), test_papers (3), test_agent (1), test_rss (1)

### 待实现

- [ ] 机器之心 RSS 端到端验证（60 分钟退避已实现；当前 429 为前期配额消耗残留，待配额重置后验证实际采集入库）
- [ ] 更多中文源：自托管 RSSHub 后填入 `RSS_FEEDS` 路由（知乎/微信公众号等）
- [ ] 部署方案（Docker 编排 / GitHub Actions 自动采集日报）——后期讨论事项待议
- [ ] 前端体验微调：yellow 主题浅/深区比例、亮黄饱和度可继续按观感迭代

---


## 迭代记录

| 日期 | 内容 |
|------|------|
| 2026-07-30 | 项目启动，需求讨论，技术选型，骨架搭建 |
| 2026-07-30 | 实现 models / base / hackernews / engine；3 个测试文件；修复 uv 镜像 |
| 2026-07-30 | 实现 filters / output / main；串通 MVP 全链路；CLI 入口；4 个测试文件全部通过 |
| 2026-07-31 | 实现 ArXiv 数据源；engine 改为 asyncio.gather 并发；main.py 增加采集/过滤统计；扩充关键词表 |
| 2026-08-01 | Papers With Code 数据源上线：paperswithcode.com 已被 HF 收购，改用 Hugging Face API（`/api/papers`）；类名 `HuggingFacePaperSource`，源名 `huggingface_papers`；新增 test_papers.py 3 个集成测试 |
| 2026-08-01 | CLI 重构：`fetch` 改为命令组 `cli` 的子命令（`python -m src.main fetch` 可用）；修复 `Got unexpected extra argument (fetch)`；三源并发实测通过 |
| 2026-08-02 | 去重逻辑优化：从纯 id 去重改为 arxiv ID 指纹去重，覆盖 arxiv ↔ pwc ↔ HN 链 arxiv 的真实跨源重复；删除死代码 `_ARXIV_SOURCES`；新增 3 个去重测试 |
| 2026-08-02 | engine 统计层：`run()` 返回 `EngineResult`（articles + source_stats + deduped_count + total_elapsed_ms），为 Web Dashboard 提供结构化统计；main.py 统计改为读 source_stats |
| 2026-08-02 | hackernews 并发化：详情拉取改为 `asyncio.gather` 并发，20 条详情从 20 RTT 降到 1 RTT |
| 2026-08-02 | main.py 修复未知源静默 return：改为 `raise click.ClickException`（退出码 1），不再静默成功 |
| 2026-08-02 | 日志 + 耗时统计收尾：`print`/`click.echo` 改为 `logging`（打 stderr），压制 httpx 请求日志，stdout 保持纯净可管道；补全 `SourceStats.elapsed_ms`（每源计时，CLI 显示"耗时 Nms"） |
| 2026-08-02 | 二阶段方向确认：从 CLI 升级为**个人 AI 研究情报助手**（Web Dashboard + AI Agent）。采集→SQLite→FastAPI→Vue 四层 + Agent 智能问答/趋势分析；记忆系统分层（用户偏好 P0 / 阅读历史 P1 / 对话摘要 P2）；后端优先，前端占位延后 |
| 2026-08-02 | ranking.py 复合排序 v1 实现：`final = 归一化score × 时间衰减(半衰期48h) × 源权重`，engine 单行替换 `sort(key=score)`；naive/aware datetime 统一处理；新增 test_ranking.py 8 用例（总计 41 用例） |
| 2026-08-02 | 二阶段启动（Pipeline 模块化）：新增 `src/pipeline/` 目录 + 4 个骨架文件（`__init__.py`/`dedup.py`/`ranking.py`/`llm.py`），仅 docstring 规划职责，无实现代码；engine 暂不拆分，保持运行 |
| 2026-08-03 | Phase 1 Agent 核心完成：`src/pipeline/llm.py`（LLM 判断+质量分，DeepSeek v4-flash，.env 配置，降级回退）；`backend/agent/prompts.py`（日期锚点+时间窗口+语言策略）；`backend/agent/tools.py`（5 工具 schema，3 实现）；`backend/agent/core.py`（tool-use 主循环，并发执行独立工具）；Agent 端到端冒烟通过 |
| 2026-08-03 | 全项目 docstring 改为 Google 风格 \| 13 文件 43 函数/方法：numpy → `Args/Returns`；中文一行 → 完整；补缺；41/41 测试不受影响 |
| 2026-08-04 | Phase 2 基础设施 \| `backend/config.py`（.env 驱动）；`backend/database.py`（sqlite3 标准库，8 函数，3 表，WAL 模式）；`src/tests/test_database.py`（22 用例）；`backend/scheduler.py`（APScheduler，start/collect_once/shutdown，启动即采集）；`src/tests/test_scheduler.py`（5 用例）；新增 apscheduler 依赖 |
| 2026-08-05 | Phase 2 API 层 \| `backend/main.py`（FastAPI + lifespan + CORS）；`backend/routers/articles.py`（GET /api/articles）；新增 fastapi + uvicorn[standard] 依赖；测试增至 **71 用例**（+5 scheduler） |
| 2026-08-05 | Phase 2 路由补齐 \| `backend/routers/sources.py`（GET /api/sources，源元数据+文章数）；`backend/routers/stats.py`（GET /api/stats，采集统计+历史明细）；`backend/routers/agent.py`（POST /api/agent/chat，DB 注入→chat→清理）；main.py 挂载 4 个 router；database.py 新增 `get_source_stats()` + `get_collection_stats()`；测试目录 `src/tests/` → `tests/` 镜像重构；路由测试 24 + 全量 95 用例 |
| 2026-08-07 | Phase 2 ChromaDB 向量索引 \| `backend/vector_store.py`（213行，8 函数，paraphrase-multilingual-MiniLM-L12-v2 嵌入）；`backend/config.py` 新增 CHROMA_DIR；pyproject.toml 新增 chromadb 依赖；Agent 搜索链路（tools/agent 路由）待接入 |
| 2026-08-11 | Phase 2 完成 \| Agent 搜索接入 ChromaDB 语义检索 + SQLite 回填摘要；scheduler 采集时间步写入向量库，agent 路由不再每次重建索引；analyze_trend / trigger_collection 从 stub 实现为真实功能；删除内存索引 `_article_index`/`register_articles`/`clear_articles`；`published_at` 改为 Unix 时间戳修复 ChromaDB `$gte` 运算；新增 `test_vector_store.py` 31 用例；全量 122 用例（115 单元 + 7 集成）；Agent 端到端连通验证通过 |
| 2026-08-11 | Phase 3 联调 + Phase 4 前端占位 \| 5 个 HTTP 端点验证通过（health/sources/articles/stats/agent）；修复嵌入模型失败重复重试阻塞 event loop 的 Bug（失败哨兵 `_embedding_failed` + `asyncio.to_thread` 隔离阻塞 I/O）；`frontend/README.md` 占位创建（Vue 3 + Vite 技术选型 + 5 页面规划） |
| 2026-08-11 | Phase 5 第 1 批：质量优化 + 记忆 P1 \| `scheduler._filter_ai_related()`（LLM 语义判断 4 线程并发，失败回退关键词）接入 collect_once 入库前过滤；Agent 隐式记录阅读历史（`tools._record_reading_history` 去重/新读在前/上限 50，挂接 get_article_detail 与 summarize_articles）；`chat()` 新增 reading_history 参数；路由从 profile 读 ID 解析为标题注入 prompt；新增 test_llm.py（10 用例）+ test_tools.py（9 用例）+ scheduler/agent/router 测试扩展；全量 147 用例（140 单元 + 7 集成） |
| 2026-08-17 | 健壮性评审修复 \| 外部 AI 评审 11 项发现全部修复：H1 .gitignore 覆盖 backend/db/（check-ignore 实证）；H2 llm.py 多 key 配置只覆盖 api_key 字段保留 base_url/timeout；H3 LLM 客户端构建失败不缓存可重试；H4 collect_once 加 asyncio.Lock 防并发采集；M1 6 处 print→logging 清零；M3 database.py docstring 修复；M4 articles 路由参数 ge/le 校验；M5 ChromaDB 空结果回退 SQLite 关键词检索（`_keyword_fallback` + fallback 标记）；M6 ChatRequest history 三重限制（20 轮/20000 字符/message 4000）；M7 风格修复；全量 153 用例（146 单元 + 7 集成） |
| 2026-08-17 | Phase 5 第 2 批：源开关 + RSS 聚合源 \| **现实受阻**：机器之心改付费数据服务、知乎热榜需登录、公共 RSSHub 实例均不可用 → 调整为通用 RSS 源插件（`src/sources/rss.py`，stdlib XML 解析兼容 RSS/Atom，RSS_FEEDS 配置，移除 beautifulsoup4）；`user_profile` 加 `selected_sources` 列 + `_migrate_user_profile()` 迁移（旧库无缝升级）；`set_profile` 修复直接调用时行不存在静默失败 Bug（INSERT OR IGNORE 保底）；scheduler `SOURCE_REGISTRY` 支持类/工厂，按选中实例化；`GET /api/sources` 返回 enabled + `PUT /api/sources/{name}` 开关（空=全选/关最后源 400/全选规范化）；全量 172 用例（164 单元 + 8 集成） |
| 2026-08-18 | 机器之心官方 RSS 接入 \| 用户申请到机器之心官方 RSS 免费额度（`https://mcp.applications.jiqizhixin.com/rss?token=...`）；验证 feed 200/RSS 2.0/80KB/标题"机器之心"；RSS 插件解析逻辑与现有单测覆盖结构一致；写入 `.env` 的 `RSS_FEEDS`，config 正确解析；SOURCE_META 描述更新为"当前含机器之心官方 RSS"；sources 测试 8/8 通过。**注意**：免费配额频繁请求触发 429 限流（测试消耗了额度），scheduler 6h 间隔应合规，待配额恢复后验证实际采集 |
| 2026-08-18 | 关键词表扩充 \| 按用户测试结论补充中文关键词「模型」「价格」（68 → 70 词）：单独"模型"此前不命中导致中文源（如机器之心）大量「XX 模型发布/价格调整」类报道在 LLM 降级时被误杀；验证"模型价格对比分析/新模型发布/价格"均命中、"Sonic Pi v5 released"不误伤；test_llm + test_scheduler 21/21 通过 |
| 2026-08-19 | Phase 5 第 3 批：前端接入完成 \| 定案复用 OpenDesign 前端项目 `endfield-ai-intel-dashboard`（Vue 3.5 + Vite + TS + Element Plus + ECharts + Pinia，明日方舟·终末地 UI 风格，三栏 Dashboard），UI 层（7 组件 + 5 视图 + 双主题）零改动，从设计目录复制干净源码到 `frontend/`（OpenDesign 原项目已回滚保持原样）；后端补 `POST /api/collect`（202 + 后台任务，`_collect_lock` 防并发）+ `SOURCE_META.category` 对齐英文枚举（tech_community/academic/chinese_media）；前端新增 `src/api/` 层（axios 6 模块）+ vite proxy `/api`→8000 + 3 个 store 的 mock→真实 API 替换（Agent 非流式适配：后端整段 answer + 前端保留流式播放模拟与本地工具标签推断；days/sources 变化自动重新拉取）；端到端验证通过（sources/stats/articles/agent 全链路真实数据）；新增 test_collect 2 用例，全量 174 用例（166 单元 + 8 集成） |
| 2026-08-19 | Phase 5 第 3 批：前端优化 \| 修复隐藏 Bug（`<v-chart>` 从未注册——趋势页 3 图表实际不渲染，vue-tsc 不校验模板组件名故 typecheck 通过）；代码分割：echarts 按需（echarts/core use() 注册 line/bar/pie）+ Element Plus 按需（unplugin-vue-components + AutoImport；ElMessage 手动 deep import 规避 resolver 包入口导入拉全量）+ manualChunks 拆 vue/echarts vendor，总包 1,633 kB → 956 kB（gzip 549 → 337 kB，-39%）；mock/data.ts 清理 410 行死代码（mock 文章/源/统计生成器，仅保留 agent 欢迎语/演示历史/快捷操作）；补 @types/node；AgentChatPanel 键盘事件类型适配按需组件；git 初始提交 6d7ee28 → origin/master；样式打磨：信息源开关改为设计稿样式（方形滑块 + 2px 细线直角轨道 + yellow 荧光）、采集按钮主题色填充（修复 endfield-theme.css 全局 `.el-button{background:transparent}` 简写覆盖 EP 变量导致的描边半成品）、yellow 主题页面背景改半透明灰（html 深灰透出层） |
| 2026-08-19 | 前端主题修复（青色）\| 输入框纯白穿透 Bug：EP 2.8 输入控件用 `var(--el-input-bg-color, var(--el-fill-color-blank))`（默认白），endfield-theme.css 覆盖特异性 (0,1,0) 与 EP 相同、被按需后加载样式压过 → 三处加固：① main.ts 将 endfield-theme.css 改为动态导入（保证晚于 EP 样式注入）② 输入/弹层/消息等 51 处选择器加 `html` 前缀提特异性至 (0,1,1) ③ `:root` 新增 46 条 EP CSS 变量映射兜底（--el-input-bg-color 等 → 项目令牌）；黄色主题输入框同步改为半透明白磨砂（rgba(255,255,255,0.07)，focus 0.1） |
| 2026-08-19 | 前端趋势图空白 Bug \| 根因：ECharts category 轴要求 series 数据 key 与 `xAxis.data` 逐字一致，TrendsView 中 xAxis 用 `M/D` 格式（8/13）而 series 用 `YYYY-MM-DD`（2026-08-13）→ 所有数据点被静默丢弃（Y 轴有刻度但无线条）；修复：series 日期 key 统一为 `M/D` 与 trendChartDates 输出一致；SQLite 实证 165 篇文章均在窗口内，数据无问题 |
| 2026-08-20 | Yellow 主题重设计（参考《明日方舟：终末地》+ 分栏明暗工业风）\| 两轮迭代：① 浅灰画布 #D0D2D6 + 全深炭灰面板 #43464C + 低饱和芥末黄 #D6BD26（浅底深卡）；② 参考新图改为**分栏明暗**——顶部/右侧终端深炭灰 #323539（内层 #1C1E20）、左侧/中间浅冷灰 #ECEEED + 纯白卡片、高饱和亮黄 #E8E237；关键机制：`[data-theme='yellow'] .spanel/.aview/.tview/.hview` 作用域覆盖 CSS 变量（--fg/--surface/--border 等），浅色区自动深色文字+白卡、深色区浅色文字，组件零改动；浅色区 EP 输入/select 强制白字（深灰底）；useTheme.ts 图表色同步亮黄系列；布局 .dash 加 gap/padding 6px 让面板浮于画布 |
| 2026-08-20 | 视图切换 Tab 修复 \| 原实现：直角矩形 `.aview__views` 容器 + 独立 `skewX(-10deg)` 平行四边形指示块 → 斜块盖不满矩形直角，左上角露深色三角；修复：yellow 主题下按钮自带斜切背景（选中亮黄填充+黑字 / 未选中深炭灰），斜边互相咬合无缝，隐藏独立指示块（display:none，JS 计算的 left/width 不受影响）；青色主题滑动指示动画不受影响 |
| 2026-08-20 | LLM 过滤截断修复 + RSS 官方额度退避 \| **LLM**：用户 .env 为 deepseek-v4-flash（推理模型），`_llm_is_ai_related`/`score_quality` 的 max_tokens=64 被 reasoning_content 占满 → content 空/JSON 截断，批量采集刷屏"LLM 判断输出无法解析"；修复 max_tokens 64→256 + `_chat_json` 空串归一化 None（静默降级）；实测长中文标题正确判定零警告，21 测试通过。**RSS**：机器之心官方免费守则确认（60 分钟/次、每日 25 次、单次 10 条），RSSSource 实现 60 分钟退避（`_backoff_active` 纯函数 + 模块级 `_last_request_ts` 跨实例共享，scheduler 每次新建实例也生效），24h 自然 ≤24 次落在每日预算内；新增 4 个退避测试（间隔内跳过/恢复/禁用/从未请求），全量 170 单元 + 8 集成 |
| 2026-08-20 | RSS 429 冷却 + LLM 调用自动重试 \| **RSS**：官方额度每日 25 次耗尽后任何请求都 429（等次日重置），60min 退避只防频率不防耗尽 → 新增 429 后 6h 长冷却（模块级 `_last_429_ts`，429 由 `_fetch_feed` 捕获设置），避免每 60 分钟白碰壁；HN 单条失败日志改 `%r` 显示异常类型。**LLM**：用户报告"今日摘要"触发 analyze_trend 两次失败——诊断：LLM 瞬时服务错误（连接/超时/5xx/429），`_execute_tool` 重试只覆盖抛异常而工具返回 error dict 不触发，LLM 层无重试 → `_chat_json` 内置指数退避重试 2 次（1s/2s，4xx 参数错误不重试），全部 LLM 调用（过滤/打分/趋势/Agent 对话）受益；新增 4 个重试测试；全量 176 单元 + 8 集成 |
| 2026-08-20 | 记忆 P2：对话摘要记忆 \| 滚动摘要窗口：`user_profile` 加 `conversation_summary`/`conversation_updated_at` 列 + `_migrate_user_profile` 迁移（旧库无缝升级）；新建 `backend/agent/memory.py`（`summarize_conversation` 增量合并——旧摘要+新轮次→新摘要，上限 300 字，失败返回 None 保留旧摘要）；`build_system_prompt` 加"跨会话对话记忆"注入段；`chat()` 加 conversation_summary 参数；`routers/agent.py` 归档接线（history 超 12 条时归档最早部分→摘要存库→请求只带最近 12 条；摘要失败优雅降级只截断不存库）；前端零改动（服务端自动压缩）；新增 test_memory 5 用例 + database 迁移/读写 2 + prompt 注入 1 + router 归档 3；端到端实测首次归档与增量合并均正确（提取偏好+保留结论，75 字）；全量 187 单元 + 8 集成 |
| 2026-08-20 | 前端测试接入（Vitest）\| 项目根 `tests/frontend/` 镜像前端结构，与 Python 测试并列；工具链：vitest + happy-dom（无 DOM 依赖组件测试，聚焦数据层）；vite.config.ts 加 `test` 配置（include 指向 ../tests/frontend/、fs.allow 放行 root 外读取、pinia alias 指向真实 node_modules 解决 root 外裸依赖解析）；package.json 加 `npm test`/`test:watch`；23 用例：api（articles 参数构造 3 / agent 截断 3）、stores（articles 归一化+watch 重拉+搜索 4 / sources 开关+400 降级 4 / agent 流式流程+失败复位 4）、utils（relativeTime 5）；`npm test` 23/23 通过 |
