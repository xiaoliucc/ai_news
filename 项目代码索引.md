# 项目代码索引

> 最后更新: 2026-08-28

---

## 目录总览

```
ai-research-intelligence/
├── src/                        # 源代码
│   ├── __init__.py             # ✅ 包标记
│   ├── main.py                 # ✅ CLI 入口 — click 命令组 cli + fetch 子命令 (132行)
│   ├── engine.py               # ✅ 调度引擎 — NewsEngine 类 + 去重 + 统计层 (251行)
│   ├── models.py               # ✅ 数据模型 — Article dataclass (31行)
│   ├── filters.py              # ✅ AI 相关性过滤 (129行, 85关键词)
│   ├── output.py               # ✅ 输出格式化 (123行)
│   ├── pipeline/               # ✅ 流水线加工模块
│   │   ├── __init__.py         # ✅ 包标记
│   │   ├── dedup.py            # 🔧 去重（预留，从 engine 移出）
│   │   ├── ranking.py          # ✅ 复合排序 (124行, 时间衰减+源权重+归一化)
│   │   └── llm.py              # ✅ LLM 语义判断+质量打分 (290行, qwen-turbo/DashScope, key 按 base_url 匹配, 瞬时错误自动重试)
│   └── sources/                # 数据源插件
│       ├── __init__.py         # ✅ 包标记（空）
│       ├── base.py             # ✅ 抽象基类 SourcePlugin (30行, 含 PROXY_URL 全局代理)
│       ├── hackernews.py       # ✅ Hacker News 采集器 (93行, 并发拉详情)
│       ├── arxiv.py            # ✅ ArXiv 采集器 (89行)
│       ├── papers.py           # ✅ Papers With Code 采集器 (100行)
│       ├── rss.py              # ✅ 通用 RSS/Atom 聚合器 (stdlib XML 解析, RSS_FEEDS 配置)
│       └── github.py           # ✅ GitHub 采集器 (305行, 解析 trending+explore 静态页, 今日 star 热度/explore score=0, 两页按 id 合并去重, bs4, 采集时刻时间戳)
├── tests/                       # 测试（镜像 src/ + backend/ 结构）
│   ├── src/                     # 对应 src/ 测试
│   │   ├── test_models.py       # ✅ Article 单元测试 (21行)
│   │   ├── test_engine.py       # ✅ Engine 单元测试 (250行, 13用例)
│   │   ├── test_output.py       # ✅ Output 单元测试 (147行, 13用例)
│   │   ├── pipeline/
│   │   │   ├── test_ranking.py  # ✅ Ranking 单元测试 (109行, 8用例)
│   │   │   └── test_llm.py      # ✅ LLM 单元测试 (10用例, LLM优先/回退/降级/打分)
│   │   └── sources/
│   │       ├── test_hackernews.py # ✅ HN 集成测试 (24行)
│   │       ├── test_arxiv.py    # ✅ ArXiv 集成测试 (37行, 2用例)
│   │       ├── test_papers.py   # ✅ Papers 集成测试 (64行, 3用例)
│   │       ├── test_rss.py      # ✅ RSS 单元+集成测试 (12用例, RSS/Atom/畸形/截断/退避/429)
│   │       └── test_github.py   # ✅ GitHub 单元测试 (14用例, 解析/回退/截断/畸形/网络降级/时间戳/explore 合并)
│   └── backend/                 # 对应 backend/ 测试
│       ├── test_database.py     # ✅ Database 单元测试 (26用例)
│       ├── test_config.py       # ✅ Config 单元测试 (5用例, 公网地址归一化/CORS 组合)
│       ├── test_scheduler.py    # ✅ Scheduler 单元测试 (11用例, 含 LLM 过滤接线/选中源实例化)
│       ├── test_vector_store.py # ✅ Vector Store 单元测试 (385行, 31用例)
│       ├── agent/
│       │   ├── test_agent.py    # ✅ Agent 单元+集成测试 (4用例, 含 reading_history/summary 注入)
│       │   ├── test_core_stream.py # ✅ Agent 流式事件序列测试 (4用例, 纯文本/tool 轮/参数拼接/不透出 JSON)
│       │   ├── test_memory.py   # ✅ 摘要记忆单元测试 (5用例, 合并/空轮次/降级)
│       │   └── test_tools.py    # ✅ 工具集单元测试 (24用例, 阅读历史+回退检索+标题回退+精确匹配+web_search)
│       └── routers/
│           ├── test_articles.py # ✅ Articles 路由测试 (7用例)
│           ├── test_sources.py  # ✅ Sources 路由测试 (8用例, enabled+PUT 开关/404/400/规范化)
│           ├── test_stats.py    # ✅ Stats 路由测试 (4用例)
│           ├── test_agent.py    # ✅ Agent 路由测试 (13用例, SSE 事件流 + history 限制/归档)
│           └── test_collect.py  # ✅ Collect 路由测试 (2用例, 202+后台任务/异常吞掉)
│   └── frontend/                 # ✅ 前端测试（Vitest 24 用例，镜像 frontend/src 结构）
│       ├── api/                 # ✅ articles 参数构造 / agent 截断
│       ├── stores/              # ✅ articles 归一化+watch / sources 开关 / agent 流式
│       └── utils/               # ✅ relativeTime 格式化
├── backend/                    # ✅ FastAPI 后端（Phase 2 完成）
│   ├── __init__.py             # ✅ 包标记
│   ├── config.py               # ✅ 配置管理（40行，.env 驱动，含 CHROMA_DIR / PROXY_URL 代理）
│   ├── database.py             # ✅ SQLite 数据层（362行，10 函数，3 表，WAL 模式）
│   ├── vector_store.py         # ✅ ChromaDB 向量索引（245行，9 函数，惰性初始化，Unix 时间戳）
│   ├── scheduler.py            # ✅ APScheduler 定时采集（167行，采集时间步写入 ChromaDB）
│   ├── main.py                 # ✅ FastAPI app（65行，lifespan + CORS + 5 router）
│   ├── routers/                # ✅ API 路由（Phase 2 完成）
│   │   ├── articles.py         # ✅ GET /api/articles（25行）
│   │   ├── sources.py          # ✅ GET /api/sources + PUT /api/sources/{name}（enabled 状态 + 开关）
│   │   ├── stats.py            # ✅ GET /api/stats（20行，采集统计+历史明细）
│   │   ├── agent.py            # ✅ POST /api/agent/chat（93行，直接调用，索引由 scheduler 维护）
│   │   └── collect.py          # ✅ POST /api/collect（202 + 后台任务，手动触发采集）
│   └── agent/
│       ├── __init__.py         # ✅ Agent 包标记
│       ├── core.py             # ✅ Agent 主循环+流式 (459行, chat() 最多5轮 tool-use + chat_stream() SSE 事件流)
│       ├── tools.py            # ✅ 工具集 (397行, 5 工具全部实现：语义搜索+摘要回填/SQLite详情/LLM摘要/趋势分析/触发采集；_resolve_article 标题回退 + search 精确匹配)
│       ├── memory.py           # ✅ 对话摘要记忆 P2（增量合并，上限 300 字，失败保留旧摘要）
│       └── prompts.py          # ✅ System prompt (92行, 日期锚点+语言策略+用户偏好+跨会话记忆)
├── frontend/                   # ✅ Vue 3 SPA（Phase 5 第 3 批完成，OpenDesign 复刻 + 真实 API）
│   └── src/
│       ├── api/                # ✅ axios 数据层（http 实例 + articles/sources/stats/agent/collect）
│       ├── stores/             # ✅ Pinia stores（articles/sources/agent/layout，真实 API）
│       ├── views/              # ✅ 5 视图（Hotlist/Articles/Trends/SourcesPanel/AgentChatPanel）
│       ├── components/         # ✅ 7 组件（ArticleCard/MessageBubble/SourceItem/StatBlock/ToolCallBadge/TopBar）
│       ├── layouts/            # ✅ DashboardLayout 三栏布局
│       ├── styles/             # ✅ variables.css（双主题令牌）+ endfield-theme.css（EP 覆写）
│       ├── utils/              # ✅ echarts 按需注册 / 图表主题 / i18n
│       └── mock/               # ✅ agent 预设（欢迎语/演示历史/快捷操作，文章 mock 已清理）
├── tests/                      # ✅ 测试目录（174 用例，镜像 src/ + backend/）
├── 项目计划.md                  # 项目计划文档
├── 项目进度.md                  # 开发进度跟踪
├── 项目Bug.md                   # Bug 记录
├── 后期讨论事项.md               # 待讨论事项
├── 项目代码索引.md              # 本文件
├── pyproject.toml              # 项目配置 (uv)
└── README.md                   # 快速入门
```

图例：✅ 已实现  🔧 进行中  ⬜ 待实现

总代码：~14,100 行（后端 4,420 + 前端 5,749 + 测试 3,990） | 测试用例：230 个

---

## 当前依赖（pyproject.toml）

| 包 | 用途 | 类型 |
|---|---|---|
| `httpx` | HTTP 异步客户端，API 请求 | 运行时 |
| `click` | CLI 命令行框架 | 运行时 |
| `arxiv` | ArXiv 官方 API 客户端 | 运行时 |
| `openai` | LLM 调用（OpenAI 兼容端点，DeepSeek） | 运行时 |
| `python-dotenv` | .env 环境变量加载 | 运行时 |
| `apscheduler` | 定时调度（进程内，AsyncIOScheduler） | 运行时 |
| `fastapi` | Web API 框架 | 运行时 |
| `uvicorn` | ASGI 服务器（含 standard 扩展：websockets, watchfiles） | 运行时 |
| `chromadb` | 向量数据库（PersistentClient + SentenceTransformer 嵌入） | 运行时 |
| `sentence-transformers` | 文本嵌入模型（chromadb 依赖，paraphrase-multilingual-MiniLM-L12-v2） | 运行时 |
| `pytest` | 测试框架 | dev |
| `pytest-asyncio` | 异步测试支持 | dev |

## CLI 入口

```bash
python -m src.main fetch -s hackernews -n 20 -o text
python -m src.main fetch -s hackernews,arxiv,huggingface_papers -n 5 -o json -f result.json
uv run ai-news fetch --limit 10
```

## 数据采集统计

运行时会输出每个数据源的采集和过滤数量：
```
[hackernews] 采集 5 条
[arxiv] 采集 5 条
--- 过滤后 ---
[hackernews] 保留 3 条
[arxiv] 保留 2 条
```

## 已知限制

- 🟡 关键词过滤偏严 — 学术标题天然不含热门词，ArXiv 命中率低。后续应引入 LLM 语义判断或放宽过滤策略
- 🟢 Agent 对话为 **SSE 真流式**（2026-08-28 完成）：后端 `chat_stream()` 事件流 + 路由 StreamingResponse，前端 fetch 增量读取，工具事件真实上报、token 级输出（模拟器已删）

---

## 文件职责

### [src/main.py](src/main.py) ✅
CLI 入口，使用 `click` 定义命令组 `cli` + `fetch` 子命令（132行）：
- `python -m src.main` — 命令组，`--help` 列出子命令
- `python -m src.main fetch` — 采集子命令
- `--sources` / `-s` — 数据源，逗号分隔。可用: hackernews, arxiv, huggingface_papers
- `--limit` / `-n` — 每源采集条数（默认 20）
- `--output` / `-o` — 输出格式：text / json / markdown（默认 text）
- `--output-file` / `-f` — 写入文件（默认终端输出）
- 自动 `sys.path` 处理；统计信息读自 `EngineResult.source_stats`；未知数据源抛 `ClickException`（退出码 1）
- 统计日志用 `logging` 打 stderr（`print`/`click.echo` 已移除），压制 httpx/httpcore 请求日志，stdout 保持纯净可管道（`-o json | jq`）

### [src/engine.py](src/engine.py) ✅
核心调度引擎 `NewsEngine` 类 + 去重 + 统计层（251行）：
- `__init__(sources)` — 注册数据源列表
- `run(limit)` — 并发执行：asyncio.gather 同时拉取所有源 → 去重 → 按热度降序排序，返回 `EngineResult`
- `_fetch_timed(source, limit)` — 单个源采集计时，返回 `(结果, 错误, 耗时秒)`，配合 gather 并发逐源计时
- `_deduplicate(articles)` — 去重：arxiv ID 指纹优先（URL 或 id 前缀提取，覆盖 arxiv↔pwc↔HN 链 arxiv 的跨源重复），否则回退原始 id，保留首次出现
- `SourceStats` / `EngineResult` dataclass — 结构化统计（每源采集数/失败/错误/耗时 + 去重消除数 + 总耗时），为 Web Dashboard 预留
- 单个源失败不影响其他源（`return_exceptions=True`）；失败通过 `logging.error` 输出（不再 print 污染 stdout）

### [src/models.py](src/models.py) ✅
`Article` dataclass — 统一数据结构（10 个字段）：
`id`, `title`, `url`, `source`, `summary`, `author`, `published_at`, `score`, `tags`, `language`

### [src/filters.py](src/filters.py) ✅
AI 相关性过滤（113行，70 关键词）：
- `AI_KEYWORDS` — 中英文关键词列表（覆盖 LLM/CV/RL/多模态/具身智能等）
- `is_ai_related(article)` — 按标题关键词匹配，大小写不敏感
- `filter_ai_articles(articles)` — 批量过滤

### [src/output.py](src/output.py) ✅
格式化输出，三个函数均返回 `str`：
- `output_text(articles)` — 纯文本格式
- `output_json(articles)` — JSON 格式，datetime 转 ISO
- `output_markdown(articles)` — Markdown 格式

### [src/sources/base.py](src/sources/base.py) ✅
`SourcePlugin` 抽象基类（ABC）：

### [src/sources/hackernews.py](src/sources/hackernews.py) ✅
Hacker News — Firebase API，`HackerNewsSource`（93行）：
- 拉取 top stories ID 后，`asyncio.gather` 并发拉取单条详情（20 RTT → 1 RTT）
- 单条失败只跳过该条（`_fetch_item` 返回 None 过滤），不中断整个源

### [src/sources/arxiv.py](src/sources/arxiv.py) ✅
ArXiv — 官方 API，`ArxivSource`（89行）：
- 使用 `arxiv` 库调用 ArXiv API
- `asyncio.to_thread` 将同步 Client 包装为异步
- 11 个 AI 相关分类（cs.AI / cs.CL / cs.CV / cs.LG 等）
- 多作者用逗号拼接；tags 存 primary_category

### [src/sources/papers.py](src/sources/papers.py) ✅
Papers With Code — `HuggingFacePaperSource`（100行）：
- Papers With Code 已被 Hugging Face 收购，底层改用 HF 官方 API `https://huggingface.co/api/papers`
- 字段映射：`publishedAt` → datetime（ISO 字符串去 Z 时区标记）、作者列表 → 逗号拼接（超 5 人加"等N人"）
- url 优先 GitHub 仓库，否则 arXiv 页面；`upvotes` → score；id 前缀 `pwc_`

### [tests/test_models.py](tests/test_models.py) ✅
Article dataclass 冒烟测试。

### [tests/test_hackernews.py](tests/test_hackernews.py) ✅
HackerNewsSource 集成测试（标记 `integration`）。

### [tests/test_engine.py](tests/test_engine.py) ✅
NewsEngine 单元测试（13 用例），使用 Mock 数据源，覆盖：去重（含 arxiv↔pwc 跨源、HN 链 arxiv、非 arxiv id 不误判）、并发采集、排序、单源失败容错、统计信息。

### [tests/test_output.py](tests/test_output.py) ✅
Output 格式化测试（13 用例）。

### [tests/test_arxiv.py](tests/test_arxiv.py) ✅
ArxivSource 集成测试（2 用例，标记 `integration`）：验证返回类型和 limit 生效。

### [tests/test_papers.py](tests/test_papers.py) ✅
HuggingFacePaperSource 集成测试（3 用例，标记 `integration`）：验证返回类型/字段完整性、limit 生效、JSON 序列化。

### [src/pipeline/](src/pipeline/) ✅
流水线加工模块（纯函数集合，engine 只做调度）：
- `__init__.py` — 包标记，说明设计原则（纯函数、单向依赖 models）
- `ranking.py` — 复合排序 `rank(articles)`：`final = 归一化score × 时间衰减(半衰期48h) × 源权重(hackernews=1.0/arxiv=0.6/pwc=0.8)`；同源 min-max 归一化；naive/aware datetime 统一 UTC；无时间戳默认衰减 0.5
- `dedup.py` — 【预留】从 engine 移出：`_strip_arxiv_version` / `_extract_arxiv_id` / `_dedup_key` / `deduplicate()`
- `llm.py` — LLM 语义判断 + 质量打分（290行）：`.env` 驱动（`python-dotenv`），OpenAI 兼容端点（qwen-turbo / DashScope，key 按 base_url 自动匹配 DASHSCOPE_API_KEY / DEEPSEEK_API_KEY）；`_chat_json` 瞬时错误（连接/超时/5xx/429）指数退避重试 2 次、4xx 不重试；`is_ai_related(article)` LLM 优先、失败回退关键词；`score_quality(article)` 返回 0-100 或 None；`response_format={"type": "json_object"}` + 代码围栏剥离

### [backend/agent/prompts.py](backend/agent/prompts.py) ✅
System prompt（92行）：
- 四段式：PERSONA / DATA_BOUNDARY / TOOL_RULES / STYLE_RULES
- `build_system_prompt(interests, reading_history, language, data_window_days)` — 参数化组装
- 日期锚点（`date.today()`）+ 时间窗口 + 语言策略（zh 正文中文+标题保留原文）

### [backend/agent/tools.py](backend/agent/tools.py) ✅
Agent 工具集（501行）：
- `TOOLS` — 6 个 OpenAI function calling schema
- `_search_articles` — ChromaDB 语义检索 + SQLite 回填摘要；空结果回退 `_keyword_fallback`（SQLite 关键词检索，带 fallback 标记）
- `_get_article_detail` — SQLite 查询文章详情；成功后隐式记录阅读历史
- `_summarize_articles` — LLM 中文概括一批文章；成功后隐式记录阅读历史
- `_analyze_trend` — 语义检索 + LLM 识别 3-5 个热点方向
- `_trigger_collection` — 手动触发 `collect_once()`
- `_web_search` — Tavily 联网搜索（采集库外实时信息）：httpx POST `api.tavily.com/search`，走全局代理，未配置 key/网络失败返回可读 error；`_parse_search_results` 纯函数过滤脏条目 + content 截断 500 字
- `_record_reading_history` — 阅读历史写入（去重、新读在前、上限 50）
- `EXECUTOR` — 6 工具全部注册，无 stub

### [backend/agent/core.py](backend/agent/core.py) ✅
Agent 主循环 + 流式（459行）：
- `chat(message, history, interests, reading_history)` — tool-use loop，最多 5 轮（非流式保留）
- `chat_stream(...)` — SSE 事件生成器：LLM `stream=True`，同步 chunk 经线程+队列桥接（`_iter_chunks`）防阻塞事件循环；工具轮发 `tool start/done` 事件（真实上报），答案轮 token 逐块 yield，结束发 `done`（含完整 answer）
- 复用 `pipeline/llm.py` 的 `_get_client()` / `_model_id()`；未配置 key 时产出 error 事件

### [tests/backend/test_vector_store.py](tests/backend/test_vector_store.py) ✅
Vector Store 单元测试（385行，31 用例）：覆盖 _document/_metadata 构建、_get_collection 单例+降级、_get_embedding_fn 惰性初始化+失败降级、add_articles/delete_article 正常+异常、search 基本检索+source/days 过滤+组合+空查询+降级、clear 正常+异常、_reset 清理。

### [tests/backend/agent/test_agent.py](tests/backend/agent/test_agent.py) ✅
Agent 单元+集成测试（184行，3 用例）：mock 测试未配置 key 提示、tool-use 循环；integration 测试全链路采集→向量库→Agent 对话。

### [tests/backend/test_database.py](tests/backend/test_database.py) ✅
Database 单元测试（417行，22 用例）：覆盖 init_db、save_articles（空/写入/覆盖/幂等/collection_run）、query_articles（时间筛选/源筛选/tags 还原/limit/空表）、get_article（命中/未命中/非法 JSON）、get_profile（默认值/幂等）、set_profile（部分更新/空参数/持久化）、_row_to_dict。

### [backend/config.py](backend/config.py) ✅
全局配置（77行）：.env 驱动，SQLITE_PATH / COLLECTION_LIMIT / COLLECTION_HOURS / PROXY_URL（全局代理）/ PUBLIC_BACKEND_URL + PUBLIC_FRONTEND_URL（内网穿透双隧道，`_normalize_url` 归一化 + `cors_origins` 放行前端隧道 Origin）。

### [backend/database.py](backend/database.py) ✅
SQLite 数据层（362行，10 函数）：sqlite3 标准库，WAL 模式，3 张表（articles / collection_runs / user_profile），含 save_articles / query_articles / get_article / get_profile / set_profile / get_source_stats / get_collection_stats + 私有辅助。首次导入自动建表。

### [backend/scheduler.py](backend/scheduler.py) ✅
APScheduler 定时采集（167行）：AsyncIOScheduler，start() 启动+立即采集，collect_once() 流程：engine.run() → `_filter_ai_related()`（LLM 语义过滤 4 线程并发，回退关键词）→ save_articles() → add_articles()（同步写入 ChromaDB）；`_collect_lock`（asyncio.Lock 防并发采集）；shutdown() 关闭。

### [backend/main.py](backend/main.py) ✅
FastAPI app（132行）：lifespan 生命周期管理（启动 scheduler、关闭清理），CORS 中间件（localhost:5173），挂载 4 个 router（articles / sources / stats / agent），`/` 根路径 + `/health` 健康检查。

### [backend/vector_store.py](backend/vector_store.py) ✅
ChromaDB 向量索引（245行，9 函数）：SentenceTransformer 嵌入（paraphrase-multilingual-MiniLM-L12-v2），惰性初始化，PersistentClient 本地持久化；_get_collection（双重检查锁）/ _get_embedding_fn（惰性单例）/ add_articles（批量 upsert）/ search（语义检索，Unix 时间戳支持 $gte 过滤）/ clear / delete_article / _reset。published_at 存为 float 时间戳兼容 ChromaDB where 运算。

### [backend/routers/articles.py](backend/routers/articles.py) ✅
GET /api/articles 路由（25行）：query params days/sources/limit → database.query_articles() → `{"total": N, "articles": [...]}`。

### [backend/routers/sources.py](backend/routers/sources.py) ✅
GET /api/sources + PUT /api/sources/{name} 路由（119行）：预定义 SOURCE_META（hackernews/arxiv/huggingface_papers/rss 共 4 源）含 label/description/category，合并 database.get_source_stats() 的 article_count；GET 每项含 `enabled`（`selected_sources` 空列表=全选=全 true）；PUT 开关（未知源 404 / 关闭最后一个 400 / 全选态规范化存 []）。

### [backend/routers/stats.py](backend/routers/stats.py) ✅
GET /api/stats 路由（20行）：limit 参数（1-100，Query 校验），透传 database.get_collection_stats()，返回 total_runs/total_articles/last_collection_at/runs 明细。

### [backend/routers/agent.py](backend/routers/agent.py) ✅
POST /api/agent/chat 路由（152行，SSE 真流式）：Pydantic 请求体（message/history/interests）；响应 `StreamingResponse`(text/event-stream)，事件协议 tool/token/done/error，流中异常以 error 事件兜底；interests 默认从 user_profile 读取；P2 对话归档（history 超 12 条压缩进 conversation_summary）；reading_history ID→标题解析。ChromaDB 索引由 scheduler 采集时维护，无需每次请求重建。

### [backend/routers/collect.py](backend/routers/collect.py) ✅
POST /api/collect 路由（36行）：手动触发全量采集，`asyncio.create_task` 后台执行 + 立即返回 202；`_background_tasks` 集合持有任务引用防 GC；异常只记日志不抛出；与 Agent 的 TRIGGER_COLLECTION 工具共用 `collect_once()`（`_collect_lock` 防并发）。

### [frontend/](frontend/) ✅
Vue 3.5 SPA（Phase 5 第 3 批，OpenDesign `endfield-ai-intel-dashboard` 复刻 + 真实 API）：
- `src/api/http.ts` — axios 实例（baseURL `/api`，timeout 120s），get/post/put 类型封装 + 拦截器解包 + ElMessage 统一报错
- `src/api/` — 5 个模块：articles（days/sources 逗号分隔/limit）/ sources（含 PUT 开关）/ stats / agent（history 截断 20 条）/ collect（202）
- `src/stores/` — articles（fetchArticles 真实 API，days/sources 变化 watch 自动重拉，summary null 兜底）/ sources（并行拉取 + 开关 + 采集后延迟 12s 刷新统计）/ agent（**SSE 真流式**：api/agent.ts fetch+ReadableStream 增量解析 + abort，tool/token/done/error 事件实时渲染，后端真实工具标签；streamSimulator 与 inferToolCalls 已删）
- `src/utils/echarts.ts` — echarts 按需注册（line/bar/pie + Grid/Tooltip/Legend + CanvasRenderer）
- `src/composables/useTheme.ts` — 双主题切换（data-theme 挂 body + localStorage 持久化）；`SERIES_COLORS`/`CHART_UI` 图表色：yellow 为分栏明暗适配（亮黄 #E8E237 主色 / tooltip 深炭灰 #323539 / 浅区刻度深灰 #55585D）
- `src/styles/variables.css` — 双主题令牌：cyan 工业青 / **yellow 分栏明暗**（画布浅冷灰 #ECEEED + 顶/右深炭灰 #323539 + 左/中浅灰白卡 + 高饱和亮黄 #E8E237）；yellow 下通过 `[data-theme='yellow'] .spanel/.aview/.tview/.hview` 作用域覆盖 CSS 变量实现浅区深色文字/白卡；`.dash` 加 gap/padding 6px 让面板浮于画布；视图切换 tab 修复（按钮自带斜切背景，隐藏独立指示块）
- `src/styles/endfield-theme.css` — EP 全量覆写（终末地 HUD：直角/斜切/细线），输入类选择器统一 `html` 前缀（0,1,1 > EP 0,1,0）防白色穿透，`:root` 映射 46 条 EP CSS 变量兜底；注意 `.el-button{background:transparent}` 全局简写会覆盖 EP 变量，需用 scoped 长写属性覆盖
- vite.config.ts — `/api` proxy→8000；manualChunks 拆 vue/echarts vendor；unplugin-vue-components + AutoImport（ElementPlusResolver，ElMessage 需手动 deep import + style/css）
- 构建：`npm run typecheck` / `npm run build`（总包 ~956 kB，gzip ~337 kB）
