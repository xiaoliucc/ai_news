# 后期讨论事项

> 最后更新: 2026-08-18

---

## 已解决

### 1. ~~关键词过滤 → LLM 语义判断~~

- **状态**: 已实现。`src/pipeline/llm.py` LLM 语义判断优先，失败回退关键词匹配。2026-08-17 已接入 scheduler 采集链（入库前过滤）

### 2. ~~持久化存储~~

- **状态**: 已实现。SQLite（WAL 模式），`backend/database.py` 管理 3 张表

### 3. ~~Web Dashboard~~

- **状态**: 已实现。FastAPI 后端已就绪（4 个路由），Vue 前端 Phase 4 占位

### 4. ~~定时调度~~

- **状态**: 已实现。APScheduler AsyncIOScheduler，采集时间步写入 ChromaDB

### 5. ~~AI Agent 智能问答~~

- **状态**: 已实现。5 工具 tool-use loop（语义搜索 + 详情 + 摘要 + 趋势分析 + 触发采集），ChromaDB 向量检索 + LLM 对话，端到端连通

---

## 待讨论

### 1. 国内数据源（2026-08-18 更新）

- ✅ **机器之心官方 RSS** — 用户申请到官方 RSS 免费额度（`https://mcp.applications.jiqizhixin.com/rss?token=...`），已接入 `.env` 的 `RSS_FEEDS`；feed 200/RSS 2.0 验证通过。**注意**：免费配额频繁请求会 429 限流，scheduler 6h 间隔应合规，避免频繁 `trigger_collection`
- ❌ 机器之心 HTML 采集 — 站点已改为**付费数据服务**，公开文章列表页下线
- ❌ 知乎热榜 — 需登录 cookie（403/401）
- ❌ 公共 RSSHub 实例 — rsshub.app 403 / 其他实例 503/404/超时
- ⏳ 知乎/微信公众号等更多中文源 — 自托管 RSSHub 后填入 `RSS_FEEDS` 路由（通用 RSS 源插件已就绪）
- ⏳ 微信公众号 — 采集难度高，暂缓

### 2. 部署方式

- 本地运行？云服务器？Docker？
- 是否需要 GitHub Actions 自动生成日报？

---

## 讨论记录

| 日期 | 内容 | 结论 |
|------|------|------|
| 2026-07-30 | 技术选型、MVP 范围 | Python + uv + click + httpx + rich；先 3 个源终端输出 |
| 2026-07-31 | MVP 端到端打通 | 2 数据源（HN + ArXiv）并发拉取，3 格式输出，5 测试文件 25 用例全部通过 |
| 2026-08-01 | 第三数据源上线 | Papers With Code 改用 HF API（`HuggingFacePaperSource`），6 测试文件 28 用例全部通过（单元 22 + 集成 6） |
| 2026-08-01 | CLI 重构为命令组 | `fetch` 变为 `cli` 子命令（`python -m src.main fetch` 可用），三源并发实测通过 |
| 2026-08-02 | 去重策略优化 | 纯 id 去重改为 arxiv ID 指纹优先（覆盖 arxiv↔pwc↔HN 链 arxiv 跨源重复），回退原始 id；暂不加 URL 归一化/fuzzy，等二阶段历史去重再定 |
| 2026-08-02 | engine 统计层 | `run()` 返回 `EngineResult`（source_stats/deduped_count/elapsed），为 Web Dashboard 预留结构化统计 |
| 2026-08-02 | 异步优化范围确认 | 仅 hackernews 详情拉取改 `asyncio.gather` 并发；arxiv（已 to_thread）与 papers（纯 CPU 转换）无真瓶颈，不改 |
| 2026-08-02 | 未知源静默 return 修复 | 改 `raise click.ClickException`（退出码 1），CI/脚本化调用不再被误判成功 |
| 2026-08-02 | 日志输出与耗时统计 | `print`/`click.echo` 改 `logging`（stderr），压制 httpx 请求日志，stdout 保持纯净可管道；补全每源 `elapsed_ms` 计时；暂不建 ranking 模块（当前排序只有一行 sort，等二阶段组合评分再抽 `ranking.py`） |
| 2026-08-02 | 二阶段：Pipeline 模块化 | 新增 `src/pipeline/`（dedup/ranking/llm 三模块 + 包标记），纯函数集合，engine 只做调度；实施顺序：ranking v1（时间衰减+源权重）→ LLM 过滤+打分（filters 退役）→ ranking v2 → 持久化 storage.py；output.py 归展示层不属 pipeline；当前仅建骨架无实现 |
| 2026-08-02 | 二阶段方向确认 | 最终定为**个人 AI 研究情报助手**：Web Dashboard + AI Agent。采集→SQLite→FastAPI→Vue 四层；Agent 做采集管理/整理/解析/问答/趋势分析（tool-use 模式，非 LangChain）；记忆分层（用户偏好 P0 / 阅读历史 P1 / 对话摘要 P2）；热榜数据定时采集存储，源管理用 UI toggle 而非动态注册 |
| 2026-08-02 | ranking v1 实现 | `final = 归一化score × 时间衰减(半衰期48h) × 源权重`；同源 min-max 归一化解决 ArXiv score=0 垫底；naive/aware datetime 统一 UTC 处理；engine 单行替换；test_ranking.py 8 用例（总计 41）；v2 预留 LLM 质量因子 |
| 2026-08-03 | Phase 1 Agent 核心 | `src/pipeline/llm.py`（DeepSeek v4-flash，OpenAI 兼容端点，.env 配置，降级回退）；`backend/agent/`（prompts 四段式+日期锚点+语言策略 / tools 5 schema+3实现 / core tool-use 循环并发执行）；Agent 端到端冒烟通过；`.env` 加入 `.gitignore` |
| 2026-08-03 | 全项目 docstring 改为 Google 风格 | 13 文件 43 函数/方法：原有 numpy 风格和中文一行 docstring 统一改为 `Args:`/`Returns:` 标准 Google 风格；补 10 处缺失 docstring |
| 2026-08-04 | Phase 2 基础设施 | `backend/config.py`；`backend/database.py`（sqlite3，3 表，8 函数）；`tests/backend/test_database.py`（22 用例）；`backend/scheduler.py`（APScheduler）；`tests/backend/test_scheduler.py`（5 用例）；新增 apscheduler 依赖；测试 41→66 用例 |
| 2026-08-05 | Phase 2 API 层 | `backend/main.py`（FastAPI+lifespan+CORS）；`backend/routers/articles.py`（GET /api/articles）；新增 fastapi+uvicorn[standard] 依赖；测试 66→71 用例（+5 scheduler） |
| 2026-08-05 | Phase 2 路由补齐 | `backend/routers/sources.py`（源元数据+动态文章数）；`backend/routers/stats.py`（采集统计+历史明细）；`backend/routers/agent.py`（DB 注入→Agent→清理）；database.py 新增 get_source_stats/get_collection_stats；测试目录重构 `src/tests/` → `tests/` 镜像布局；全量 95 用例 |
| 2026-08-07 | Phase 2 ChromaDB 向量索引 | `backend/vector_store.py`（213行，8 函数，paraphrase-multilingual-MiniLM-L12-v2 嵌入）；Agent 搜索链路（tools/agent 路由）待接入 — Phase 3 Plan 已设计 |
| 2026-08-11 | Phase 2 完成 | Agent 搜索接入 ChromaDB + SQLite 回填摘要；scheduler 采集时间步写 ChromaDB；analyze_trend/trigger_collection 实现；删除内存索引；published_at Unix 时间戳 Bug 修复；test_vector_store.py 31 用例；全量 122 用例 115 通过；Agent 端到端连通验证通过 |
| 2026-08-17 | Phase 5 第 1 批 + 健壮性评审 | 确认后续方向：质量优化/记忆P1/机器之心/前端，分批递进；**CLI 开发暂停**，专注前端+FastAPI 后端；LLM 过滤接入 scheduler（A1）+ 阅读历史 Agent 隐式记录（B1）；外部评审 11 项（H1-H4/M1-M7）全部修复，含 ChromaDB 空结果 SQLite 关键词回退、history 三重限制；全量 146 用例通过 |
| 2026-08-17 | Phase 5 第 2 批：源开关 + RSS | 国内源调研：机器之心改付费、知乎需登录、公共 RSSHub 不可用 → 定案**自托管 RSSHub + 通用 RSS 源插件**（RSS_FEEDS 配置）；user_profile 加 selected_sources 列 + 迁移；PUT /api/sources/{name} 开关（空=全选/关最后 400/规范化）；scheduler 按选中实例化；set_profile 补 INSERT OR IGNORE 修静默失败；全量 172 用例通过 |
| 2026-08-18 | 机器之心官方 RSS | 用户申请到机器之心官方 RSS 免费额度并接入 `.env`；feed 格式验证通过；因免费配额频繁请求触发 429 限流（测试消耗额度），scheduler 6h 间隔应合规；知乎等其余中文源仍待自托管 RSSHub |
