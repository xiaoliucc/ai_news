# 项目计划

> 最后更新: 2026-08-18

---

## Context

构建一个 AI 圈热点新闻收集 Agent，自动聚合多个渠道的 AI 热点内容。

**目标：** 综合多个新闻来源（海外技术社区 + 国内媒体/社区 + 学术论文），先做 MVP 原型（Python），终端输出即可，后续迭代为更复杂的形态。

---

## MVP 范围

### 核心功能（第一期）

1. 从 3 个来源采集 AI 相关新闻/论文
2. 统一数据格式，去重
3. 按热度/时间排序
4. 终端表格/文本输出

### 预留扩展点

- 更多数据源插件
- LLM 摘要/翻译/分类
- 数据库存储
- Web Dashboard 展示
- 定时调度

---

## 架构

```
ai-research-intelligence/
├── src/
│   ├── __init__.py
│   ├── main.py              # CLI 入口 (click 命令组 cli + 子命令 fetch)
│   ├── engine.py            # 调度引擎：编排采集、去重、排序
│   ├── models.py            # 统一数据模型 (Article dataclass)
│   ├── pipeline/            # 【二阶段】流水线加工模块
│   │   ├── __init__.py
│   │   ├── dedup.py         # 去重（从 engine 移出）
│   │   ├── ranking.py       # 复合排序（时间衰减 + 源权重 + 质量分）
│   │   └── llm.py           # LLM 语义判断 + 质量打分
│   ├── sources/             # 数据源插件目录
│   │   ├── __init__.py
│   │   ├── base.py          # 抽象基类 SourcePlugin
│   │   ├── hackernews.py    # Hacker News API
│   │   ├── arxiv.py         # ArXiv API
│   │   ├── papers.py        # Papers With Code (Hugging Face API)
│   │   └── rss.py           # 通用 RSS/Atom 聚合（RSS_FEEDS 配置，中文源）
│   ├── filters.py           # AI 相关性过滤（llm.py 上线后退役）
│   └── output.py            # 输出格式化器
├── backend/                 # 【二阶段】FastAPI 后端
│   ├── main.py              # FastAPI app + CORS
│   ├── config.py            # 配置管理（DB路径/LLM key/采集间隔）
│   ├── database.py          # SQLite 连接 + 建表（articles/collection_runs/user_profile）
│   ├── vector_store.py      # ChromaDB 向量索引（Agent RAG）
│   ├── scheduler.py         # APScheduler 定时采集
│   ├── agent/               # AI Agent 核心（LLM + tool-use loop）
│   │   ├── core.py          # Agent 主循环
│   │   ├── tools.py         # 工具集（search/summarize/analyze_trend）
│   │   └── prompts.py       # System prompt
│   └── routers/             # REST 路由（articles/sources/stats/agent）
├── frontend/                # 【占位】Vue SPA（Phase 4 实现）
├── tests/
├── pyproject.toml
└── README.md
```

---

## 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| CLI 框架 | `click` | 成熟稳定，装饰器风格 |
| HTTP 客户端 | `httpx` | 异步支持，API 友好 |
| HTML 解析 | `beautifulsoup4` | 需要爬取页面时使用 |
| 数据模型 | `dataclasses` | 类型安全，轻量 |
| 输出格式化 | `rich` | 终端表格/面板美观 |
| 异步运行时 | `asyncio` | 并发采集多个数据源 |
| 包管理 | `uv` | Rust 实现，比 pip 快 10-100x |
| 定时调度 | `apscheduler` | 进程内 AsyncIOScheduler，比系统 cron 更易维护 |
| 数据库 | `sqlite3`（标准库） | 零配置单文件，WAL 模式，适合个人使用 |
| API 框架 | `FastAPI` | Python 异步原生，自动 OpenAPI 文档 |
| ASGI 服务器 | `uvicorn` | 轻量高性能，支持 --reload 热重载 |

---

## 数据模型

```python
@dataclass
class Article:
    id: str                    # 唯一标识 (source:source_id)
    title: str
    url: str
    source: str                # hackernews / arxiv / huggingface_papers
    summary: str
    author: str | None
    published_at: datetime | None
    score: int                 # 热度 (upvotes / citations)
    tags: list[str]            # AI / LLM / CV / NLP...
    language: str              # en / zh
```

---

## 数据源采集策略

### Phase 1 — 3 个源

| 来源 | 内容 | 接口 |
|------|------|------|
| Hacker News | Top Stories 中过滤 AI 相关帖子 | Firebase API，免费无认证 |
| ArXiv | cs.AI / cs.CL / cs.CV / cs.LG 新论文 | 官方 API，免费无认证 |
| Papers With Code | AI 领域论文 + 代码实现 | Hugging Face API（`/api/papers`），免费无认证 |

### 后续扩展

- ✅ 机器之心 — 官方 RSS（免费额度，`RSS_FEEDS` 配置，429 限流）
- ⏳ 知乎热榜、36Kr、微信公众号 — 自托管 RSSHub 后填 `RSS_FEEDS` 路由
- Twitter/X（需 API key）
- Reddit r/MachineLearning（JSON API）— 已被 Papers With Code 替代

---

## CLI 设计

```bash
# 默认采集所有源，终端表格输出
python -m src.main fetch

# 指定来源 / 数量
python -m src.main fetch --sources hackernews,arxiv --limit 30

# 输出到文件
python -m src.main fetch --output markdown --output-file daily.md

# 可用数据源
python -m src.main sources list
```

---

## 实现步骤

1. **项目脚手架** — 目录结构、pyproject.toml、CLI 入口
2. **核心模型 & 引擎** — models.py、base.py、engine.py
3. **数据源插件** — hackernews.py、arxiv.py、papers.py
4. **输出 & 过滤** — filters.py、output.py
5. **测试验证** — 手动运行 + 集成测试

---

## 验证方式

```bash
uv sync
uv run python -m src.main fetch --limit 20
# 预期：输出 20 条 AI 相关热点，包含来自 HN、ArXiv、Papers With Code 的内容
```

---

## 二阶段：Web Dashboard + AI Agent 智能平台

**目标**：从 CLI 工具升级为**个人 AI 研究情报助手**——后台定时采集多源 AI 新闻，SQLite 存储，FastAPI 查询，Vue 前端展示一周热榜；AI Agent 负责智能收集整理、文章解析、RAG 问答、趋势分析。

**架构**（四层）：
```
采集层(Scheduler+Engine) → 存储层(SQLite+ChromaDB) → API层(FastAPI) → 展示层(Vue SPA)
                                                    ↘ AI Agent(LLM+Tools) ↗
```

**实施顺序**（后端优先，前端延后）：
- [x] Phase 0: ranking.py 复合排序 v1（时间衰减 + 源权重 + 归一化）
- [x] Phase 1: Agent 核心（`backend/agent/` LLM+tool-use）+ `pipeline/llm.py`
- [x] Phase 2: FastAPI 骨架（SQLite ✅ / routers ✅ / Scheduler ✅ / ChromaDB ✅ / Agent 搜索接入 ✅ / 趋势分析 + 触发采集 ✅ / 索引策略优化 ✅）
- [x] Phase 3: 联调验证（Agent 搜索 → 采集入库 → 查询）
- [x] Phase 4: 前端占位（`frontend/` 仅 README，不做实现）
- [x] Phase 5 第 1 批: 质量优化 + 记忆 P1（LLM 语义过滤接入 scheduler / Agent 隐式记录阅读历史）
- [x] Phase 5 第 2 批: 源开关 + RSS 聚合源（`selected_sources` + 通用 RSS 插件 + 机器之心官方 RSS）
- [x] Phase 5 第 3 批: 前端接入（2026-08-19 完成：复用 OpenDesign 前端项目 `endfield-ai-intel-dashboard`）
  - 技术栈与计划一致（Vue 3.5 + Vite + TS + Element Plus + ECharts + Pinia + vue-router），UI 层（7 组件 + 5 视图 + 双主题）零改动
  - 类型定义已对齐后端（Article/Source/CollectionStats/ChatMessage），数据层为 mock，需替换为真实 API
  - 接入工作：① 后端 `POST /api/collect` 新端点（手动触发采集，Agent 工具之外补齐 REST）② `SOURCE_META.category` 对齐前端英文枚举（tech_community/academic/chinese_media）③ 前端 axios + `src/api/` 层 + 3 个 store 的 mock→真实 API 替换 ④ vite proxy `/api`→8000 ⑤ Agent 非流式适配（后端返回整段 answer，前端保留流式播放模拟 + 本地工具标签推断）

**关键决策**：
- Agent 用 tool-use loop（非 LangChain），openai SDK 直接实现
- LLM 用 OpenAI 兼容端点（DeepSeek v4-flash），.env 配置，python-dotenv 加载
- 记忆系统：用户偏好（P0，`user_profile` 表）→ 阅读历史（P1）→ 对话摘要（P2）
- 源管理：UI toggle 可选源，非动态注册
- 热榜数据：定时采集 + SQLite 存储

**engine.py 演进**：
```
run(): gather 拉取 → deduplicate() → rank() → EngineResult
```
