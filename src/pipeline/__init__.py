"""
流水线加工模块 — 负责数据源采集之后、输出之前的各阶段处理。

设计原则：
    - 每个模块是纯函数集合，无副作用，易于测试
    - engine.py 只做调度编排，调用这里的阶段函数
    - 依赖方向：pipeline → models（单向）

阶段模块：
    dedup.py    — 去重（从 engine 移出，后续）
    ranking.py  — 复合排序（时间衰减 + 源权重 + 质量分）
    llm.py      — LLM 语义判断 + 质量打分（后续）
"""
